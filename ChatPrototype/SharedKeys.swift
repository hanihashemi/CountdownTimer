import Foundation

enum Shared {
    static let appGroup = "group.io.github.hanihashemi.ChatPrototype"

    enum Keys {
        static let endDate = "countdown.endDate"
        static let startedAt = "countdown.startedAt"
        static let title = "countdown.title"
    }
}

